# 2026-09-14 私有候选版

这是给维护者做安装验证的候选成品，不是面向普通用户正式开放的版本。仓库保持私有，正式网页地址尚未确定；请勿把附件转为公开下载或用真实私人资料试装。

## 附件

- `mind-universe-cloudflare-pages.zip`：Cloudflare Pages 静态前端。
- `mind-universe-netlify-static.zip`：Netlify 静态前端。
- `mind-universe-cloudflare-backend.zip`：个人 Cloudflare 后台、数据库迁移和开发者安装器。
- `SHA256SUMS.txt`、`release-manifest.json`：下载后校验文件完整性；校验值不是发布者数字签名。

前端的两个 ZIP 内容相同，按托管平台分别命名，避免选错。每包 867 个文件、最大单文件约 11.1 MiB，已按 Cloudflare 当前直接上传文件限制核对；这不代表已经实际上传平台。ZIP 只放 Release 附件，不进入 Git 历史。

候选源码提交：`6c642f1f5a5ef7cc3a68f85dc1baaf75e27bfb56`。完整源码仍在原私有工程，不随此仓库提供。网页 BUILD 沿用开发基线 209，不能靠 BUILD 将本候选版与现有线上版混为一谈；请以本页候选日期、提交与附件 SHA-256 识别。后台协议为 1，后台包版本为 0.1.0。

## 已验证

- 个人后台设置、地址与配对码连接、同实例重配恢复、不同后台凭据隔离、故障不回退作者后台。
- 模型、TTS、实时和持久后台的路由/认证自动测试；前端相关 217 项，个人 Worker 的 11 项本地 workerd 测试，原后台 166 项回归。
- Chromium 与 WebKit 的配对、刷新保留、失败提示和说明页往返；相关设置/导航 25 项组件测试。
- 完整前端构建、独立后台构建、Functions 类型检查；Netlify 33 项相关测试、三个真实函数 ZIP 的本机 HTTP 冒烟。
- 开发者安装工具的 5 项本地维护测试、打包器 14 项验证；最终后台 ZIP 解压后实际运行随包检查器通过，三个 ZIP 和清单的 SHA-256 校验通过。
- 一次成功的真实模型回复，经过编译个人网关取得“连接已验证。”；首次参数不兼容返回明确 400，修正测试参数后成功，没有重试未知收费结果。
- 产物检查没有发现原工程目录、source map、受检查规则覆盖的疑似密钥或作者后台域名字面量。压缩 JavaScript 仍可被查看和分析，不承诺不可逆向。

## 尚未完成，不能据此宣称可正式安装

1. 真实 Cloudflare 账号的首次安装、升级、故障恢复与云端资源检查。当前安装器需要电脑上的 Node 和 Wrangler，仅供开发者内测；“只用手机登录后自动安装”没有实现。
2. 真实语音合成、实时通话、完整业务保存及 iPhone 主屏幕、锁屏和推送验收。自动测试中的模拟语音不是真实语音服务验收。
3. 正式前端网址、大陆网络可达性测试，以及素材、字体和实际第三方打包内容的公开分发许可复核。
4. 面向普通用户的最终简化安装路线；电脑首次安装是否接受仍待产品决定。

以上未完成项不会通过把 Cloudflare 管理凭据交给维护者中转来绕过。现有线上网站没有因此更新，也没有替用户创建云资源或开通付费服务。

## 内测操作边界

只做文件检查时，解压后台包后运行 `node check-package.mjs` 即可，不会创建云资源。真正部署前先读包内 `DEPLOY.md`，确认自己的账号、允许访问的网页地址与费用；不要把后台安装秘密填进 PWA 或 GitHub。

更新、停用前先在旧实例停止后台消息并收取已有结果，保留原安装记录与密钥。不要重新生成加密密钥，也不要以删除云数据库代替安全停机。正式发布仍以 [发布前检查](RELEASE_CHECKLIST.md)为准。
